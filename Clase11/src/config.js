//info sensible que no debería subirse al repositorio, debería añadirse este archivo al .gitignore
const PORT = 8080
const STRC = "mongodb://127.0.0.1"
const NAMEBASE = "prueba"
const PERSISTENCE = "MONGO"

export default {
    PORT,
    STRC,
    NAMEBASE,
    PERSISTENCE
}
